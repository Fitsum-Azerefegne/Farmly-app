import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF006B2D);
  static const background = Color(0xFFF4F8F1);
  static const backgroundLight = Color(0xFFF3FBF3);
  static const white = Colors.white;
  static const text = Color(0xFF2F2F2F);

  // darker primary used for headings/button accents
  static const darkPrimary = Color(0xFF1B5E20);
  // success/secondary green used in some components
  static const success = Color(0xFF2E7D32);
  // muted text colors
  static const muted = Color(0xFF667085);
  static const mutedLight = Color(0xFF98A2B3);
  // subtle shadow (approx 4% black)
  static const shadow = Color(0x0A000000);
}
